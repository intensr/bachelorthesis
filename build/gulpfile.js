'use strict';
// copy (fonts), newer, sourcemaps js, sourcemaps min.css -> sass/css, .on('error'), gulp default
// var plugins = require('gulp-load-plugins')();
var gulp = require('gulp');

require('./gulp/tasks/watch.js');
